import { Story, Meta } from '@storybook/react';

import FooterComp from './index';

export const Footer: Story = () => <FooterComp />;

export default {
  title: 'Footer',
  component: FooterComp,
} as Meta;

import styled from '@emotion/styled';

export const IconSm = styled.img`
  width: 22px;
  height: 22px;
  color: var(--colors-iconGray);
  fill: var(--colors-iconGray);
`;

export default function Icon({}) {
  return <i data-feather="circle"></i>;
}

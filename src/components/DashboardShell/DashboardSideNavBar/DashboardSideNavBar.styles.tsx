import styled from 'styled-components';

export const DashboardSideNavBarContainer = styled.nav`
  padding: var(--size-4);
  padding-top: 0;
  padding-left: 0;
  position: sticky;
  top: 2rem;
`;

export const DashboardSideNavList = styled.ul`
  margin: 0;
  padding: 0;
  width: var(--size-full);
  display: flex;
  flex-direction: column;
  gap: 2ch;
  margin: 0;
`;

import styled from 'styled-components';

export const HeaderNavbarContainer = styled.nav`
  width: var(--size-full);
  padding: var(--size-4) 0;
  margin-bottom: var(--size-4);
  display: flex;
  align-items: center;
  justify-content: space-between;
  a {
    text-decoration: none;
    color: var(--color-text);
  }
`;
